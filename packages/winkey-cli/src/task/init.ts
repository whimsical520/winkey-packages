import {  } from 'winkey-log'
import LocalConfig from '../lib/localConfig'
import type { ActionSturct } from '../model/action'

const localConfig = new LocalConfig()

export const initAction = async (_, cmder: ActionSturct) => {
 
}